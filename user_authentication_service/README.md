# User authentication service

SQLAlchemy `User` model and Flask API for user registration and sessions.

## Dependencies

```bash
pip3 install -r requirements.txt
```

## Task 0 – User model

Run `python3 main.py` from this directory to print the `users` table mapping.
